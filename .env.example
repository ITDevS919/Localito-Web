# Localito Marketplace Frontend - Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's already in .gitignore

VITE_API_URL=http://localhost:5000/api
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Map embeds (business location). If set, business profile shows an embedded map.
# Use one or both. Google Maps Embed API key: https://console.cloud.google.com/
# Mapbox: https://account.mapbox.com/access-tokens
# VITE_GOOGLE_MAPS_API_KEY=your_google_maps_embed_api_key
# VITE_MAPBOX_ACCESS_TOKEN=your_mapbox_public_token
